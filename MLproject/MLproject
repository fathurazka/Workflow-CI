name: msml-kriteria-3

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      max_iter: {type: int, default: 1000}
      dataset : {type: string, default: "dataset.csv"}
    command: "python modelling.py {max_iter} {dataset}"